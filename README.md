# rust-npm

A WebAssembly npm package built with Rust and wasm-pack.

## Installation

```bash
npm install rust-npm
```

## Usage

```javascript
import { greet } from 'rust-npm';

greet('World');
```

This will display an alert with "Hello, World!" in the browser.